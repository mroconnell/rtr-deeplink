# Dashboard checklist — 2026-08-31

Auto-generated by the weekday-morning routine from `BACKLOG.md`'s `[HUMAN]`
items (scope: "Needs a human" section + the Search Console/SEO subsection
only — platform/jurisdiction coverage items are out of scope for this
routine). 5 items, all Search Console, all under a minute each. Every item
below was re-checked against `git log` / `BACKLOG_DONE.md` this morning and
still looked open. **Items 3 and 4 have since been worked through live
with Ryan and are done** — left in place below with their resolution
noted, rather than deleted, so the file still explains what was checked
and why.

## 1. Validate Fix — thumbnailUrl "Videos" structured data

**Step 1 — Search Console → Enhancements → Videos report.**
Open the "Missing field `thumbnailUrl`" issue. The flagged URL is
`https://redtaperecordings.com/m/lynchburg-va-2024-08-13-city-council-special-called-meeting-august-13-2024`
(not San Carlos — an earlier guess in the backlog was wrong and already
corrected). Its `thumbnailUrl` is confirmed live and correct as of
2026-08-29. Click **VALIDATE FIX**. As of the 2026-08-29 check this still
read "Not Started" — nobody has clicked it yet.

## 2. Validate Fix — "video isn't on a watch page"

**Step 1 — Search Console → Indexing → find the "video isn't on a watch
page" issue** (under the Video indexing / Enhancements report — exact menu
location may have moved; search for that issue title if not obvious).
Two of the underlying fixes — IQM2's hardcoded `video_format` bug and the
`/j/*`/`/state/*` pages' VideoObject→CreativeWork retype — deployed the
evening of 2026-08-30 and were confirmed live 2026-08-31. This item is
no longer deploy-pending — click **VALIDATE FIX** now.
**Don't expect 100% clearance.** ~65% of the original failing population
is third-party infrastructure this app's code can't fix and won't clear
regardless: Granicus blocks Googlebot's User-Agent outright (51.6%), and
CivicClerk's `cpmedia.azureedge.net` serves video as
`Content-Disposition: attachment` (13.4%). A partial clear is the expected
good outcome, not a sign the fix didn't work.

## 3. DONE 2026-08-31 — two Soft 404 pages: recrawl cleared both

`/m/beaufort-board-of-education-academics-committee` and
`/m/city-of-carrollton-2022-10-25-city-council-on-2022-10-25-5-45-pm`
both now show clean Page indexing (✅) in URL Inspection — no Soft 404
flag on either. Ryan pasted the actual crawled HTML for both: real,
coherent `VideoObject`/`Event` JSON-LD and a genuine transcript on each
(Beaufort: ~1,394 real transcript segments, a real June 17 board
meeting — not garbled, correcting an earlier BACKLOG.md claim that both
were). A recrawl clearing an unchanged page favors the transient
truncated-proxy-bug theory over "Google judged it genuinely low-value."
Full write-up: `BACKLOG_DONE.md`'s "Two Soft 404 pages" entry.

**Separate finding surfaced while checking this**: Beaufort's URL
Inspection still shows Events: 1 invalid / Videos: 1 invalid (a
different report — rich-results validity, not indexing). Traced to the
already-known Granicus-blocks-Googlebot's-User-Agent issue from item #2
above (the Granicus media stream itself fails Googlebot's fetch, visible
in the page's "Page resources couldn't be loaded" panel) — not a new
bug, not actionable in code.

## 4. DONE 2026-08-31 — "Page indexed without content" (welcome-to-clerkbase) cleared

Ryan pasted the Enhancements "Detected items" view for this URL: the
real meeting behind its frozen slug ("February 7, 2022 - Regular Village
Council Meeting," Yellow Springs OH) shows "Crawled successfully," only
6 non-critical optional-field warnings (organizer, eventStatus, endDate,
offers, performer, address). Content is present — the flag has cleared.
Full write-up: `BACKLOG_DONE.md`'s matching entry.

## 5. Get the URL list — "Not found (404)"

**Step 1 — Search Console → Indexing → Pages → "Why pages aren't
indexed" → "Not found (404)" row.** This is the one category from the
original "Reasons preventing pages from being indexed" alert
(2026-08-23) that's never had an actual URL list — the alert emails only
ever named categories, never URLs. Open the row and note/export the URLs.
What to look for: a fix shipped and went **live today (2026-08-31)** for
reslugged pages that were serving a `200` instead of a real `301` for
their old permalink (`_SLUG_REDIRECTS` / `proxy_get()` bug, confirmed via
`curl` before/after — see `BACKLOG_DONE.md`'s `[Done 2026-08-31]` entry).
If any of these old permalinks show up in this "Not found (404)" list,
that's expected and should clear on its own once Google re-crawls and
sees the `301`. Genuine 404s (unknown slugs, never reslugged) are
expected here and are not a bug. Search Console data usually lags a
deploy by days, so this list likely won't reflect today's fix yet — if it
looks unchanged from a prior check, that's not a failure, just re-check
in a future export.

---

**Left off this list on purpose** (checked, but not dashboard-glance
material):
- The 2026-08-23 state/hub rebuild's indexing-verdict measurement needs
  "a few weeks" of Search Console data post-rebuild — today is only 8 days
  out, not due until roughly mid-September. Nothing to check yet.
- Two `[HUMAN]` items are product/judgment decisions with no dashboard
  step, not glance items: deciding the `/meetings` result link order from
  GA4 click data (needs real traffic volume first, not just a login), and
  what — if anything — to build for fake/spoofed government-content
  detection (Ryan's open call, no dashboard involved).
- The Clerk `user.deleted` → `saved_items` purge has never been fired
  end-to-end, but confirming it means actually deleting a real test
  account and verifying rows disappear — a deliberate test action, not a
  quick glance, so left off this list.

No items found to be already resolved that still had live BACKLOG.md
entries (checked `git log --oneline -20` and grepped `BACKLOG_DONE.md` for
each candidate above).
