# Dashboard checklist — 2026-09-18

Auto-generated by the weekday-morning routine. Source: `BACKLOG.md`'s
`[HUMAN]` items (the "Needs a human" section) plus the "Reliability, ops
& cost" → "Search Console, structured data & SEO plumbing" subsection.
The "Platform & jurisdiction coverage"-shaped work (jurisdiction
extraction & backfill, adapter & platform gaps) was skipped entirely —
other agents work there.

Of "Needs a human"'s 17 entries, only the Render bandwidth item below is
a real dashboard glance — the rest need a production API/delete call, a
script run from a Render Shell, a deploy + backfill, direct info only
Ryan has, or a pure product judgment call with no dashboard step, so
they were left off (see BACKLOG.md itself for those).

**2 items survive the filter, plus 1 flagged as likely already done.**
Should take under two minutes total.

---

## 1. Render bandwidth overage — size the actual cost

**Backlog entry**: `BACKLOG.md`, "Needs a human" → "Production actions
only Ryan should take" (grep `Render account bandwidth hit its 25
GB/month`).

**Background**: two real Render alerts, confirmed against Gmail. All
times below are **UTC** (double-checked, not local): "Approaching
Bandwidth Limit" (>70% of 25 GB) on **2026-09-09 12:09 UTC**, day 9 of
the billing cycle, then "Your Render Account Reached the Bandwidth
Limit" (100%) on **2026-09-12 12:09 UTC**, day 12. Per the alert's own
text, everything past that point bills at **$15/100GB** until the cycle
resets. Not yet sized — this checklist item is exactly that sizing
step.

**Step 1 — Render → Billing.**
Go to `https://dashboard.render.com/w/tea-d21a0h24d50c739htil0/billing`.
Look for the **Monthly Included Usage** breakdown, specifically the
bandwidth-by-category table.
- **Cycle has reset and bandwidth is back under 25 GB**: no cost is
  accruing right now — note the reset date so a session can close this
  entry in `BACKLOG.md`.
- **Still over 25 GB this cycle**: read the overage dollar amount shown,
  and which category/service is driving it. Video/transcript proxying is
  the standing guess, but this table is the only way to confirm it
  rather than guess.

**Step 2 — hand the answer to a follow-up session.**
Whatever Step 1 shows (reset already happened, or still over and by how
much / driven by what) is what's needed to close out or act on this
`BACKLOG.md` entry — note it back into the backlog thread or tell a
session directly.

---

## 2. Search Console "Missing field" flags — `uploadDate` (Videos), `startDate` (Events)

**Backlog entry**: `BACKLOG.md`, "Reliability, ops & cost" → "Search
Console, structured data & SEO plumbing" (grep `New "Missing field"
flags`).

**Background** (from the entry, so there's nothing to re-derive): a
2026-08-21 code fix made a page with a null/unparseable `date` emit
*neither* `uploadDate` nor `startDate` in its structured data, instead
of emitting an invalid value — trading Search Console's old "invalid
datetime value" flag for a new "missing field" flag on the same
underlying pages. The flag first appeared in Search Console 2026-08-31.
Re-verified today: still open, no closing entry in `BACKLOG_DONE.md`, no
further changes to `archive/templates/meeting_page.html` since. Same
shape as the `thumbnailUrl` "Missing field" flag that closed itself out
in Search Console once its own fix landed (`BACKLOG_DONE.md`'s
2026-08-31 entry) — this is that same kind of check, for the newer flag.

**Step 1 — Google Search Console → Videos enhancement report.**
Go to `search.google.com/search-console`, select the
redtaperecordings.com property. In the left sidebar under
"Enhancements," click **Videos**. Look for an issue row reading
**"Missing field 'uploadDate'"** and note its **Affected items** count.
- **0 or not listed**: the gap isn't showing up as a live issue today —
  nothing to do; worth noting the entry as low-priority/closeable.
- **A nonzero, meaningful count**: confirms the trade-off is real at
  scale. Worth a follow-up session running `GET
  /internal/date-format-audit` (needs the `ARCHIVE_INGEST_TOKEN` bearer
  token) to get the exact null/unparseable-date page count before
  deciding whether to backfill, fix per-adapter date capture, or accept
  the gap — see the backlog entry for the full curl command.

**Step 2 — Google Search Console → Events enhancement report.**
Same property, left sidebar "Enhancements" → **Events**. Look for
**"Missing field 'startDate'"** and note its **Affected items** count.
Same read as Step 1: zero/absent means low priority, a real count means
the same audit-endpoint follow-up applies.

No further UTC/local-time conversion needed here — the dates involved
(2026-08-21 fix, 2026-08-31 flag) are calendar dates only, no
timestamps to convert.

---

## Flagged: possibly already resolved — verify before spending time on this

### 4 LocalView channels needing a yes/no

`BACKLOG.md` ("Needs a human" → "Production actions only Ryan should
take") still carries: "4 LocalView channels from WO-175's recheck read
as an official government channel in the right state, but the name is
not an exact match — needs a person to say yes or no"
(`@CityofSantaClara`, `@JeffCityCouncil`, `@haltrammell`,
`@AbingtonTownship`).

**Evidence this is stale**: `BACKLOG_DONE.md`'s **WO-189** entry (Done
2026-09-11) shows all 4 of these exact channels got final verdicts —
`research/wo175_channel_recheck.csv` row-by-row: `@CityofSantaClara` →
pin fixed to Santa Clara, CA, no new video needed; `@JeffCityCouncil` →
queued after probe (Jeffersonville, IN); `@haltrammell` → rejected, no
page; `@AbingtonTownship` → queued after probe (Abington Twp, Montgomery
Co PA). WO-189's own "Files" note says explicitly: "`BACKLOG.md` (closed
the resolved '4 LocalView channels' ... entries)." The live
`BACKLOG.md` entry looks like a stale duplicate that was never actually
removed (a bad merge, or an edit that got reverted) — not new,
unresolved work.

**Not included as a checklist task above** for two reasons: it isn't a
dashboard lookup in the first place (it'd be "read 4 YouTube channel
pages and judge each"), and the evidence says it's already done. A human
or a later session should confirm against
`research/wo175_channel_recheck.csv`'s final verdicts and then delete
this entry from `BACKLOG.md` rather than treating it as open work.
