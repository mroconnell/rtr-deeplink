# Dashboard checklist — 2026-09-16

Auto-generated by the weekday-morning routine. Source: `BACKLOG.md`'s
`[HUMAN]` items — the "Needs a human" section (including its "Production
actions only Ryan should take" and "Decisions about already-live content"
subsections) plus the "Reliability, ops & cost" → "Search Console,
structured data & SEO plumbing" subsection, plus every other `[HUMAN]`
item found outside "Platform & jurisdiction coverage" (that section is
skipped entirely — other agents work there). Note: the TOC's expected
"Confirmations nobody has actually watched happen" subsection no longer
exists under that name — the file has been restructured since. "Production
actions only Ryan should take" is its closest current match and is fully
covered below.

Filtered to items that are a pure "log into an external dashboard, look
something up" glance — no product decisions, no destructive actions
(deletes/reslugs/DB writes), no script runs needing shell/token access.

**2 items survive the filter.** Should take a couple minutes total.

Everything else reviewed and excluded, with why:
- "How stale is too stale for a tier-3 queue candidate?" — policy call
  (a freshness cutoff), not a dashboard lookup.
- "101 West Virginia towns..." and "45 of 51 transcribed=true rows..." —
  research/judgment calls with no dashboard step.
- 13 of "Production actions only Ryan should take"'s 14 entries — each
  needs a mutating admin API call, a Render-Shell script run, or a
  product decision (mint a government, delete a page, pick a pin), not a
  glance. Only the bandwidth entry (below) is pure dashboard-read.
- "Decisions about already-live content" (repetition-loop transcript
  defects) — judgment call, no dashboard step.
- "Nothing verifies a submitted URL is a genuine government site" — open
  product-roadmap call, not a dashboard check.
- "YouTube captions via YouTube's official API" — a research/legal
  investigation task, not a dashboard check.

---

## 1. Render bandwidth overage — size the actual cost

**Step 1 — Render dashboard → Billing → Monthly Included Usage.**
Go to `https://dashboard.render.com/w/tea-d21a0h24d50c739htil0/billing`.
Look for the bandwidth breakdown table (by category — video/transcript
proxying is the suspected driver but hasn't been confirmed from the
dashboard itself). The account hit its 25 GB/month Pro-plan cap on day 12
of the billing cycle: "Approaching Bandwidth Limit" (>70%) fired
**2026-09-09 12:09 UTC**, then "Bandwidth Limit Reached" (100%) fired
**2026-09-12 12:09 UTC** (both timestamps are already UTC, taken straight
from the Gmail alerts — no conversion needed). Overage now bills at
$15/100GB until the cycle resets.

- **If the breakdown shows a clear new driver** (a route, a service) —
  that's the finding; note it so a follow-up fix can be scoped.
- **If it just shows steady/expected usage** — the cap itself may need
  raising, or this is a one-cycle blip; no code fix implied either way.

This is a **different** cause than two earlier, already-closed bandwidth
issues — don't assume either old explanation without checking the
dashboard first:
- The August double-billed-proxy cause (`ARCHIVE_BASE_URL` routing
  public traffic through the Archive over the public internet) was fixed
  2026-08-30 by moving it to Render's private network.
- An earlier version of this same alert type turned out to have a wrong
  stated limit (5 GB claimed vs. 25 GB real) — already corrected.

**Source**: `BACKLOG.md`, "Production actions only Ryan should take" →
"Render account bandwidth hit its 25 GB/month Pro-plan cap on day 12 of
the cycle." Re-verified still open 2026-09-16: no `BACKLOG_DONE.md` entry
or later commit closes it (last touch, `f84644e`, only *promoted* it into
`BACKLOG.md`, didn't close it).

---

## 2. Search Console "Missing field" flags — `uploadDate` (Videos), `startDate` (Events)

**Background** (from the entry, so you don't have to re-derive it): a
2026-08-21 code fix made a page with a null/unparseable `date` emit
*neither* `uploadDate` nor `startDate` in its structured data, instead of
emitting an invalid value — trading Search Console's old "invalid
datetime value" flag for this "missing field" flag on the same
underlying pages. First appeared in Search Console 2026-08-31. Still
open as of 2026-09-16: no `BACKLOG_DONE.md` entry closes it, and the
`/internal/date-format-audit` endpoint it names still hasn't been run
against production (needs the `ARCHIVE_INGEST_TOKEN`, which no
interactive session here has).

**Step 1 — Google Search Console → Videos enhancement report.**
Go to `search.google.com/search-console`, select the
redtaperecordings.com property. In the left sidebar, under
"Enhancements," click **Videos**. Look for an issue row reading
**"Missing field 'uploadDate'"** and note its **Affected items** count.
- **0 or not listed**: the gap isn't showing up as a live issue —
  nothing to do, this entry can be marked low-priority/closeable.
- **A nonzero, meaningful count**: confirms the trade-off is real at
  scale. Worth a follow-up session running
  `GET /internal/date-format-audit` (needs the `ARCHIVE_INGEST_TOKEN`
  bearer token) to get the exact null/unparseable-date page count before
  deciding whether to backfill, fix per-adapter date capture, or accept
  the gap — see the backlog entry for the full curl command.

**Step 2 — Google Search Console → Events enhancement report.**
Same property, left sidebar "Enhancements" → **Events**. Look for
**"Missing field 'startDate'"** and note its **Affected items** count.
Same read as Step 1: zero/absent means low priority, a real count means
the same audit-endpoint follow-up applies.

No UTC/local-time conversion needed for this entry — the dates involved
(2026-08-21 fix, 2026-08-31 flag) are calendar dates only, no timestamps
to convert.

**Source**: `BACKLOG.md`, "Reliability, ops & cost" → "Search Console,
structured data & SEO plumbing" (grep `New "Missing field" flags`).

**Heads up, not part of this checklist**: `CLAUDE_INBOX_TRIAGE.md`'s
2026-09-15 entry found a related-but-separate new flag, "Missing field
'location'" on Events, root-caused to `meeting_page.html` only emitting
`location` when `page.jurisdiction` is set. It hasn't been promoted into
`BACKLOG.md` yet, so it's out of scope for this routine (promotion needs
a human or an explicitly-asked session, per this repo's convention) —
flagging here only so it isn't missed if you're already in the Events
report for Step 2.

---

That's the whole list for today.
