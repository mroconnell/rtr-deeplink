# Dashboard checklist — 2026-08-31

Auto-generated by the weekday-morning routine from `BACKLOG.md`'s `[HUMAN]`
items (scope: "Needs a human" section + the Search Console/SEO subsection
only — platform/jurisdiction coverage items are out of scope for this
routine). 4 items, all Search Console, all under a minute each. Every item
below was re-checked against `git log` / `BACKLOG_DONE.md` this morning and
still looked open.

## 1. Validate Fix — thumbnailUrl "Videos" structured data

**Step 1 — Search Console → Enhancements → Videos report.**
Open the "Missing field `thumbnailUrl`" issue. The flagged URL is
`https://redtaperecordings.com/m/lynchburg-va-2024-08-13-city-council-special-called-meeting-august-13-2024`
(not San Carlos — an earlier guess in the backlog was wrong and already
corrected). Its `thumbnailUrl` is confirmed live and correct as of
2026-08-29. Click **VALIDATE FIX**. As of the 2026-08-29 check this still
read "Not Started" — nobody has clicked it yet.

## 2. Validate Fix — "video isn't on a watch page"

**Step 1 — Search Console → Indexing → find the "video isn't on a watch
page" issue** (under the Video indexing / Enhancements report — exact menu
location may have moved; search for that issue title if not obvious).
Two of the underlying fixes — IQM2's hardcoded `video_format` bug and the
`/j/*`/`/state/*` pages' VideoObject→CreativeWork retype — deployed the
evening of 2026-08-30 and were confirmed live 2026-08-31. This item is
no longer deploy-pending — click **VALIDATE FIX** now.
**Don't expect 100% clearance.** ~65% of the original failing population
is third-party infrastructure this app's code can't fix and won't clear
regardless: Granicus blocks Googlebot's User-Agent outright (51.6%), and
CivicClerk's `cpmedia.azureedge.net` serves video as
`Content-Disposition: attachment` (13.4%). A partial clear is the expected
good outcome, not a sign the fix didn't work.

## 3. Check recrawl — two Soft 404 pages that aren't actually thin

**Step 1 — Search Console → URL Inspection.** Check these two URLs
(Request Indexing was sent for both 2026-08-29):
- `/m/beaufort-board-of-education-academics-committee`
- `/m/city-of-carrollton-2022-10-25-city-council-on-2022-10-25-5-45-pm`

If the recrawl has landed, open **View Crawled Page** on either one — that's
the one check that tells apart a truncated-proxy response bug from Google
genuinely judging the content low-value (two different bugs, different
fixes). If it's still "pending"/not yet recrawled, nothing to do — check
again in a few days. If the Soft 404 flag has cleared on its own without
you needing to open View Crawled Page, that weakly favors the
truncated-proxy theory (a genuine low-value judgment wouldn't change from
an identical re-fetch) — worth a one-line note back in BACKLOG.md either
way.

## 4. Check recrawl — "Page indexed without content" (welcome-to-clerkbase)

**Step 1 — Search Console → Indexing → Pages → "Page indexed without
content".** Look for `/m/welcome-to-clerkbase` (Request Indexing sent
2026-08-29). If it's cleared, nothing further to do — close the BACKLOG.md
entry. If it's still flagged, leave it; still genuinely waiting on
Google's own recrawl timeline.

---

**Left off this list on purpose** (checked, but not dashboard-glance
material):
- The 2026-08-23 state/hub rebuild's indexing-verdict measurement needs
  "a few weeks" of Search Console data post-rebuild — today is only 8 days
  out, not due until roughly mid-September. Nothing to check yet.
- Two `[HUMAN]` items are product/judgment decisions with no dashboard
  step, not glance items: deciding the `/meetings` result link order from
  GA4 click data (needs real traffic volume first, not just a login), and
  what — if anything — to build for fake/spoofed government-content
  detection (Ryan's open call, no dashboard involved).
- The Clerk `user.deleted` → `saved_items` purge has never been fired
  end-to-end, but confirming it means actually deleting a real test
  account and verifying rows disappear — a deliberate test action, not a
  quick glance, so left off this list.

No items found to be already resolved that still had live BACKLOG.md
entries (checked `git log --oneline -20` and grepped `BACKLOG_DONE.md` for
each candidate above).
