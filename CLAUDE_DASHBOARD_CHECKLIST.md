# Dashboard checklist — 2026-09-15

Auto-generated by the weekday-morning routine. Source: `BACKLOG.md`'s
`[HUMAN]` items (Needs a human section) plus the Reliability/ops Search
Console subsection. Scope note: the "Needs a human" section's `Production
actions only Ryan should take` and `Decisions about already-live content`
subsections (13 + 1 entries) were reviewed and **none qualified** — every
one needs either a production API/delete call, a script run from a Render
Shell, a deploy + backfill, personal knowledge only you have, or a product
judgment call (mint a government? queue CITISTAT? delete a page?), not a
quick dashboard glance. Same for the two other `[HUMAN]` items found
outside "Needs a human" (`Trust, safety & data quality`'s URL-verification
gap, and `Roadmap & strategy`'s YouTube-captions-API investigation) — both
are open-ended product/research calls. The platform & jurisdiction coverage
areas (adapter & platform gaps, jurisdiction extraction & backfill) were
skipped entirely per scope — other agents work there.

**2 items survive the filter.** Should take a few minutes total.

---

## 1. Render bandwidth overage — size the actual dollar cost

**New since the last checklist (2026-09-11).** Re-confirmed open this
morning: today's inbox-triage pass (2026-09-15) still lists Render
bandwidth noise as a recurrence of this same tracked issue — nobody has
looked at the dashboard yet.

**Backlog entry**: `BACKLOG.md`, "Needs a human" → "Production actions
only Ryan should take" (grep `Render account bandwidth hit its`).

**Step 1 — Render → your workspace → Billing.**
Go to `https://dashboard.render.com/w/tea-d21a0h24d50c739htil0/billing`.
Look for the **Monthly Included Usage** table and its bandwidth category
breakdown.

Two real alerts caused this (from Gmail, `label:rtr-claude Bandwidth` —
both timestamps below are **UTC**; convert to your own local time before
reading them as "when it happened," don't read them as-is):
- "Approaching Bandwidth Limit" (over 70% of 25 GB) — 2026-09-09 12:09
  UTC, day 9 of the billing cycle.
- "Your Render Account Reached the Bandwidth Limit" (100%) — 2026-09-12
  12:09 UTC, day 12 of the cycle.

Since the second alert, any further usage bills at $15/100GB until the
cycle resets. Nobody has looked at the real dollar exposure yet — this
dashboard page is the only place that shows it (it's auth-walled to you).

**Step 2 — same page, read what's driving it.**
Check the bandwidth-by-category rows in that Monthly Included Usage table
(same table `BACKLOG_DONE.md`'s August "Render bandwidth" entry used).
- If one service/route dominates → that's the lead for what's newly
  pushing 25 GB/month, now that August's known cause (the resolver
  proxying the whole public site through the Archive over the public
  internet, `ARCHIVE_BASE_URL`) is already fixed (2026-08-30, moved to
  Render's private network). This alert is confirmed **not** a repeat of
  that fix — don't assume the same cause without checking.
- If it just looks like ordinary growth → the open question becomes a
  plan call (Pro plan's 25 GB included vs. $15/100GB overage vs. a bigger
  plan) rather than a bug to chase.

**Step 3 — optional, only while you're already in there: a possible side
effect.** A separate alert fired 2026-09-13 17:59 UTC (about 30 hours
after the cap was hit): the Archive service's HTTP health check timed out
once (5s timeout) — the first time in two weeks since a fix (WO-80)
resolved this exact failure mode back in August. Nobody's confirmed
whether the bandwidth cap caused this or it's unrelated noise. If the
usage graph shows a real throttling/overage spike right around 2026-09-13
17:59 UTC, that's your confirmation; if the graph is flat there, it's
probably just noise.

**What to do with the answer**: note the $ overage and what's driving it
in BACKLOG.md's entry (or tell whoever's working the backlog next) —
that's the "next action" the entry is already waiting on.

---

## 2. Search Console "Missing field" flags — `uploadDate` (Videos), `startDate` (Events)

**Carried over from the 2026-09-11 checklist — still unconfirmed.** The
backlog entry itself still reads "History: none yet," so this check has
not actually been done. Not the same thing as today's separate, already
root-caused "Missing field 'location'" Events flag (traced by this
morning's inbox triage to the known no-jurisdiction-pages gap already
tracked elsewhere in `BACKLOG.md` — no dashboard check needed for that
one, it's just noted here so you don't confuse the two).

**Backlog entry**: `BACKLOG.md`, "Reliability, ops & cost" → "Search
Console, structured data & SEO plumbing" (grep `New "Missing field"
flags`).

**Background** (from the entry, so you don't have to re-derive it): a
2026-08-21 code fix made a page with a null/unparseable `date` emit
*neither* `uploadDate` nor `startDate` in its structured data, instead of
emitting an invalid value. That traded Search Console's old "invalid
datetime value" flag for a new "missing field" flag on the same
underlying pages. The flag first appeared in Search Console 2026-08-31.

**Step 1 — Google Search Console → Videos enhancement report.**
Go to `search.google.com/search-console`, select the
redtaperecordings.com property. In the left sidebar, under
"Enhancements," click **Videos**. Look for an issue row reading
**"Missing field 'uploadDate'"** and note its **Affected items** count.
- **0 or not listed**: the gap isn't showing up as a live issue —
  nothing to do, can note this entry as low-priority/closeable.
- **A nonzero, meaningful count**: confirms the trade-off is real at
  scale. Worth a follow-up session running
  `GET /internal/date-format-audit` (needs the `ARCHIVE_INGEST_TOKEN`
  bearer token) to get the exact null/unparseable-date page count before
  deciding whether to backfill, fix per-adapter date capture, or accept
  the gap — see the backlog entry for the full curl command.

**Step 2 — Google Search Console → Events enhancement report.**
Same property, left sidebar "Enhancements" → **Events**. Look for
**"Missing field 'startDate'"** and note its **Affected items** count.
Same read as Step 1: zero/absent means low priority, a real count means
the same audit-endpoint follow-up applies.

No time-of-day/UTC conversion needed for this entry — the dates involved
(2026-08-21 fix, 2026-08-31 flag) are calendar dates only, no timestamps
to convert.
