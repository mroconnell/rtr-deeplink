# Dashboard checklist — 2026-09-01

Auto-generated by the weekday-morning routine from `BACKLOG.md`'s
`Needs a human` section and the `Search Console, structured data & SEO
plumbing` subsection. 2 items survived the dashboard-glance filter; both
still look open (checked against `BACKLOG_DONE.md` and recent commits,
see each item's "Verified still open" line). Everything else scanned
either needed engineering/API access (not a dashboard step) or was a
durable product-decision call (Ryan's judgment, not a quick glance) —
excluded per this routine's scope.

Note: the routine's own instructions expected a `Needs a human` >
`Confirmations nobody has actually watched happen` subsection. That
subsection doesn't exist in the current `BACKLOG.md` — it's been
reorganized since. This checklist covers what's actually there now:
`Needs a human`'s two entries plus the SEO-plumbing subsection.

---

## 1. Click Validate Fix in Search Console (reslug fix)

**Source**: `BACKLOG.md` → Needs a human → Production actions only Ryan
should take.

**Verified still open**: the underlying redirect fix (`_SLUG_REDIRECTS`
now sends a real 301 instead of a silent 200) deployed and was verified
live 2026-08-31 (`BACKLOG_DONE.md`). No later commit or `BACKLOG_DONE.md`
entry records the Validate Fix click itself happening — this is
distinct from the already-closed `thumbnailUrl` flag (cleared on its own
via normal recrawl, no click needed) and the already-closed "3 Search
Console checklist items" from 2026-08-25ish (Soft 404, welcome-to-clerkbase,
Granicus rich-result link — PR #635). This one's still outstanding.

**Step 1 — Search Console → Coverage/Indexing report (Pages)**
Go to Google Search Console for `redtaperecordings.com` → the Pages
(indexing) report. Find the issue category tied to the reslugged pages
(the one that flagged them as broken/not-found before the fix).
Click **Validate Fix**.

**What each answer means**: Clicking starts Google's re-check cycle —
don't expect it to clear 100% of affected URLs immediately (per the
2026-08-30 investigation writeup in `BACKLOG_DONE.md`, some residual
count is expected and normal). Nothing further to do here today beyond
clicking it — this is a "start the clock" action, not a wait-and-verify
one.

---

## 2. Search Console — "Not found (404)" category, reslugged-permalink 301 check

**Source**: `BACKLOG.md` → Needs a human → Reliability, ops & cost →
Search Console, structured data & SEO plumbing.

**Verified still open**: three of four original categories (from the
2026-08-23 alert) are resolved/explained. The fourth — "Not found
(404)" — is still open. The relevant code fix (real 301 redirects for
reslugged old permalinks, instead of a silent 200-with-different-canonical)
deployed and was verified live via `curl` on 2026-08-31
(`BACKLOG_DONE.md`, `_SLUG_REDIRECTS now sends a real 301...`). No
`BACKLOG_DONE.md` entry or commit since then records checking a fresh
Search Console export for the effect of that fix — still a real open
step.

**Step 1 — Search Console → Pages → "Not found (404)"**
Go to Search Console for `redtaperecordings.com` → Pages report → the
**Not found (404)** category. Pull the current URL list/export.

**Step 2 — Check whether any reslugged old permalinks now show as
redirects instead of 404s**
For any URL in that list that looks like an old/frozen slug (a
permalink from before a page was reslugged — see `BACKLOG_DONE.md`'s
"Five frozen-slug pages reslugged" entry, 2026-08-28, for the known
list, `welcome-to-clerkbase` included), spot-check it:
`curl -D- https://redtaperecordings.com/m/<old-slug>` — expect
`HTTP/2 301` with a `location:` header pointing at the new slug (this is
what shipped 2026-08-31; it was `HTTP/2 200` before that fix).

**What each answer means**:
- If the old permalinks are gone from the "Not found (404)" list, or
  show as 301s when spot-checked → the fix is working as intended;
  genuine 404s in the remaining list are expected/correct behavior for
  truly de-indexed content — nothing to fix.
- If a reslugged old permalink still shows up as a genuine 404 (not a
  301) → that's a real regression worth a fresh `BACKLOG.md` entry with
  the specific URL.
- **Don't touch** the other two original categories ("Alternate page
  with proper canonical tag", "Duplicate, Google chose different
  canonical than user") — those are the deliberate, documented result of
  `state_page.html:10-17` and `meeting_list.html:8-11`'s canonicalization
  choices, not bugs.

---

## Excluded from this list (for reference, not action items)

- **"New 'Missing field' flags — Videos `uploadDate`, Events `startDate`"**
  (Search Console SEO plumbing) — needs a `curl` against the resolver's
  `/internal/date-format-audit` endpoint with the `ARCHIVE_INGEST_TOKEN`
  bearer token, not a dashboard login/click — out of scope for this
  routine's "dashboard glance" filter.
- **"Repetition-loop transcript-defect population" residual work** —
  engineering/script work (extending a repair script, trimming specific
  transcripts), not a dashboard step.
- **"Nothing verifies a submitted URL is a genuine government site"**
  — explicitly "Ryan's call on what to build next," a durable
  product-decision item with no dashboard step attached.
