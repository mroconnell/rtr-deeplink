# Dashboard checklist — 2026-09-10

Auto-generated by the weekday-morning dashboard-checklist Routine. Six
items survived filtering (dashboard-glance only, judgment/destructive
calls excluded) and re-verification against `BACKLOG_DONE.md` /
`CLAUDE_INBOX_TRIAGE.md` / recent commits — all still look open. Each
step is copy-paste ready; just log in and look/click.

Note: `BACKLOG.md`'s `Needs a human` section is still reorganized from
what this routine's own instructions describe (there's no
`Confirmations nobody has actually watched happen` subsection today,
same as prior runs) — this checklist covers what's actually there now:
`Needs a human`'s "Production actions only Ryan should take" entries
plus the SEO-plumbing subsection. Four new items appeared since the
2026-09-04 checklist (PR history): two Render deploy-confirmation asks,
the SIGABRT crash-log investigation, and the GitHub secret-scanning
dismissal — none of these existed in `BACKLOG.md` as of that run.

---

## 1. Search Console → Validate Fix for the reslug fix

**Step 1 — Google Search Console → Pages (Indexing)**
Go to the property for `redtaperecordings.com` → **Pages**. Find the
validation you (or a prior session) started for the reslugged-permalink
fix (deployed and confirmed live 2026-08-31). If you see an open
validation still in progress for a "Not found (404)" or "Page with
redirect" issue tied to reslugged pages, that's this one — skip to
Step 2. If no such validation exists yet, open the relevant issue group
and click **Validate Fix**.
**Step 2 — Read the validation status.**
"Validation started"/"in progress" = nothing more to do today, it's
running. "Validation passed" = done, this checklist item is closed —
tell a later session to move `BACKLOG.md`'s "Click Validate Fix in
Search Console for the reslug fix" entry to `BACKLOG_DONE.md`. Don't
expect 100% clearance — some of the flagged URLs are expected 404s
(genuinely de-indexed content), not the reslug bug; see
`BACKLOG_DONE.md`'s `[Investigated 2026-08-30]` writeup for why.

*BACKLOG.md source: "Needs a human → Production actions only Ryan
should take" → "Click Validate Fix in Search Console for the reslug
fix." Same item as the 2026-09-04 checklist — still no commit or
`BACKLOG_DONE.md` entry recording this click having happened.*

---

## 2. Render → Archive service deploy history (WO-80 + FK-cleanup fix)

**Step 1 — Render dashboard → `rtr-deeplink-archive` service → Events (or Deploys) tab**
Look at the most recent successful deploy's timestamp. The fix needs a
build **at or after 2026-08-30 13:28 PDT (2026-08-30 20:28 UTC)** — that's
when `archive/main.py`'s `/api/health` switched to a `LIMIT 1` query and
`crud.delete_meeting_pages_by_slug()` started deleting `SocialPost`/
`MeetingPageThumbnail` rows before the page (PR #577). Both fixes are
confirmed still present on `main` as of 2026-09-05.
**Step 2 — Decide.**
Latest deploy timestamp ≥ 2026-08-30 20:28 UTC → already shipped, no
action — flag the `BACKLOG.md` entry for a later session to move to
`BACKLOG_DONE.md`. Latest deploy older than that (or `autoDeploy` never
triggered since) → click **Deploy latest commit** for this service.

*BACKLOG.md source: "...Two Archive fixes merged 2026-08-30 (WO-80's
O(1) health check, `delete_meeting_pages_by_slug()`'s FK cleanup) may
still not be deployed — confirm and redeploy if not."*

---

## 3. Render → Transcription worker deploy history (WO-88 CivicClerk fix)

**Step 1 — Render dashboard → `rtr-transcription-worker` service → Events (or Deploys) tab**
Look at the most recent successful deploy's timestamp. The fix needs a
build **at or after 2026-08-31 13:14:37 UTC (2026-08-31 06:14:37 PDT)** —
the timestamp of transcription job 1308
(`kaysville-ut-2023-04-28-city-council-work-session`), which failed 3/3
on chunk 0/14 with the exact pre-fix `mediaStreamPath` relative-path bug
(`app/platforms/civicclerk.py:77`, confirmed present on `main`).
**Step 2 — Repeat Step 1 for `rtr-transcription-worker-2`** (the second
worker service, `render.yaml`) — check independently, its deploy history
can differ from the first worker's.
**Step 3 — Decide, per worker.**
Deploy timestamp ≥ 2026-08-31 06:14:37 PDT → already shipped for that
worker. Older, or no deploy since → click **Deploy latest commit** for
that worker service. If both workers are already current, flag the
`BACKLOG.md` entry for a later session to move to `BACKLOG_DONE.md`.

*BACKLOG.md source: "...WO-88's CivicClerk `mediaStreamPath`
relative-path fix may not be deployed to the transcription worker
services — confirm and redeploy if not."*

---

## 4. Render → `rtr-deeplink` crash logs + memory graph (SIGABRT/status 134)

**Updated count since the `BACKLOG.md` entry was last written
(2026-09-05, "13 times"): today's inbox-triage run (2026-09-10) puts the
running total at 24 "Exited with status 134" alerts since 2026-08-30,
and separately tracks 16 UptimeRobot outages with *no* matching Render
alert at all — both numbers keep climbing. This is the single
highest-value item on this list.**

**Step 1 — Render dashboard → `rtr-deeplink` service (`srv-d9qhdobm8hqs738fgkog`) → Logs, filtered around known crash windows**
Pull the real exit/crash traceback (not just the restart line) around
these UTC windows — converted to PDT (UTC−7) for reference since none of
these has a matching alert to anchor on for two of the three real
confirmed outages:
- 2026-09-01 11:57:22–12:07:31 UTC (2026-09-01 04:57:22–05:07:31 PDT) —
  matched a Render alert, ~10 min outage.
- 2026-09-04 22:42:30–22:47:34 UTC (2026-09-04 15:42:30–15:47:34 PDT) —
  **no matching alert**, ~5 min outage on
  `rtr-deeplink.onrender.com/api/health/resolve-check`.
- 2026-09-04 23:34:12–23:39:17 UTC (2026-09-04 16:34:12–16:39:17 PDT) —
  **no matching alert**, ~5 min outage on `redtaperecordings.com`
  itself.
- The one crash already correlated with a manual redeploy: instance
  `8kp2j`, 8:05 AM **local** (Pacific) — this one's already in local
  time, no conversion needed.

**Step 2 — Render dashboard → `rtr-deeplink` service → Metrics tab**
Look at the memory-usage graph across the same windows above. Look for a
spike toward the plan's memory ceiling (service is on `plan: standard`
since the 2026-08-25/26 investigation) right before each crash —
that's the signal that would confirm the allocator-abort-under-memory-
pressure hypothesis over a native-extension fault (aiohttp/uvloop/
asyncpg/PyAV are the C-extension deps in play).
**Step 3 — While in Events/Deploys for this service, also check whether
PR #795** (`handle_head_requests` Content-Length fix, merged
2026-09-09 — fixes a real `RuntimeError: Response content shorter than
Content-Length` on HEAD requests to `/` and `/api/health/resolve-check`,
a plausible partial explanation for the "no matching alert" outages)
**has been deployed.** If not, it's a reasonable one to bundle into the
same deploy as any fix from this investigation.
**Step 4 — Report back** whatever the logs/graph show (or paste them for
a later session) — this Routine's own tools can't reach Render's
dashboard, so this step can only happen with you at the keyboard.

*BACKLOG.md source: "...`rtr-deeplink` (the production resolver) has
SIGABRT-crashed (status 134) at least 13 times since 2026-08-30..."
(stale count — see updated total above, from
`CLAUDE_INBOX_TRIAGE.md`'s 2026-09-10 entry).*

---

## 5. GitHub → Dismiss secret-scanning alert (confirmed false positive)

**Step 1 — GitHub → `mroconnell/rtr-deeplink` → Security tab → Secret scanning**
Find the alert flagging a "Google API Key" at
`tests/fixtures/civicplus/durham_agendacenter_citycouncil.html:103`.
**Step 2 — Confirm and dismiss.**
This is Durham NC's own public `GoogleMapsKey`, captured in a real
live-fetched HTML fixture (this repo's normal testing convention) — not
an RTR credential. The alert's own "Public leaks" section already lists
the identical key in five unrelated public repos. Dismiss it with reason
**"Used in tests."**

*BACKLOG.md source: "...Dismiss the GitHub secret-scanning alert on
`tests/fixtures/civicplus/durham_agendacenter_citycouncil.html:103` —
confirmed false positive, no RTR secret involved."*

---

## 6. Search Console → re-check "Not found (404)" export for reslugged permalinks

**Step 1 — Google Search Console → Pages (Indexing) → "Not found (404)" category**
Export the current URL list for this category.
**Step 2 — Cross-check against the reslugged pages** (the five pages
reslugged 2026-08-28, plus any reslugged since — see
`BACKLOG_DONE.md`'s "Five frozen-slug pages reslugged" entry and item 1
above). Look for whether any of their **old** permalinks now show up
classified as a redirect (301) rather than a plain 404, now that the
2026-08-31 redirect fix has landed. Spot-check with `curl -D-
https://redtaperecordings.com/m/<old-slug>` — expect `HTTP/2 301` with a
`location:` header, vs. the pre-fix `HTTP/2 200`.
**Step 3 — Decide.**
If old permalinks now show as 301s → the redirect fix is confirmed
working end-to-end in Search Console's own data; flag for a later
session to update/close the entry. If they still show as flat 404s →
worth a closer look at whether the redirect fix actually covers those
specific URLs.
**Note:** don't touch the *other* two "Alternate page with proper
canonical tag" / "Duplicate, Google chose different canonical" categories
in this same report — those are the deliberate, documented consequence
of `state_page.html`/`meeting_list.html`'s canonicalization, not a bug.

*BACKLOG.md source: "Reliability, ops & cost → Search Console,
structured data & SEO plumbing" → "'Reasons preventing pages from being
indexed' — three of four categories settled, one still open." Same item
as the 2026-09-04 checklist — still open.*

---

## Excluded (not dashboard-glance — left for direct judgment/coding sessions)

- 18 hosts tied to the wrong government / `www.sussex.nj.us` pin — needs
  writing `tenant_overrides.csv` rows and running `backfill_gov_id.py`,
  not a look-and-click.
- 13 dead-video YouTube pages — needs a noindex-vs-delete product call.
- Repetition-loop transcript-defect residual work — dev/script task,
  tagged `[NEEDS-AUDIT]` not `[HUMAN]`.
- "New Missing field" flags (uploadDate/startDate) — needs an
  `ARCHIVE_INGEST_TOKEN` curl call this Routine doesn't hold, not a
  dashboard look.
- "Nothing verifies a submitted URL is a genuine government site" /
  "YouTube captions via official API" — open-ended product-direction
  calls with no dashboard step.
- Everything under `BACKLOG.md`'s jurisdiction/platform-coverage
  sections — explicitly out of scope for this Routine (other agents work
  there).
