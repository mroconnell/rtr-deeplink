# Dashboard checklist — 2026-09-08

Auto-generated by the unattended dashboard-checklist Routine. Each item
below is a `BACKLOG.md` `[HUMAN]` entry filtered down to "log into a
dashboard, look something up, click a button, or read a graph" — durable
product-judgment calls and destructive production actions were
deliberately excluded, and stay in `BACKLOG.md` for a real working
session. Re-verified against `BACKLOG.md`, `BACKLOG_DONE.md`, and
`CLAUDE_INBOX_TRIAGE.md` as of this run — none showed evidence of already
being resolved. All timestamps below are UTC; convert to your local zone
(or switch Render's dashboard to UTC if it offers that) before hunting for
them in a time-series graph.

---

## 1. Render → `rtr-deeplink` crash logs + memory graph (SIGABRT/status-134, ongoing)

**Step 1 — Render dashboard → `rtr-deeplink` service → Events/Logs tab.**
Go to the `rtr-deeplink` service (the production resolver, `srv-d9qhdobm8hqs738fgkog`).
Filter/search logs for "Exited with status 134" around these UTC windows —
prioritize the most recent two since they're most likely still in log
retention:
- **2026-09-08 05:29:17–05:53:27 UTC** — two UptimeRobot outages back-to-back
  (`redtaperecordings.com` down 05:29:17→05:34:23; `rtr-deeplink.onrender.com/api/health/resolve-check`
  down 05:43:15→05:53:27), with **no matching Render alert** within the
  usual window (nearest alert is 06:32:31 UTC, 39–58 min later — too far to
  be the same event).
- **2026-09-04 22:42:30–22:47:34 UTC** and **2026-09-04 23:34:12–23:39:17 UTC**
  — two more confirmed outages, also with no matching alert.
Look for: the actual abort traceback (application stdout only shows the
GC-finalizer aftermath — "Unclosed client session," "netlink descriptor"
noise — not the cause). Finding a traceback here is the one thing that
would let this investigation move past "it's probably memory pressure or a
native-extension fault" and into a real fix.

**Step 2 — Render dashboard → `rtr-deeplink` service → Metrics tab.**
Look at the memory-usage graph across the same windows. This service is on
`plan: standard` after a prior starter→standard bump; if memory is pegging
near the plan ceiling right before a crash, that confirms the
memory-pressure hypothesis (it recurred despite the WO-80 health-check fix
that was supposed to relieve it). If memory looks flat/healthy right
before a crash, that points toward the native-extension-fault hypothesis
instead (aiohttp/uvloop/asyncpg/PyAV are this app's only C-extension deps).

**Why this matters**: this is now a sustained, worsening pattern — **20
"Exited with status 134" alerts since 2026-08-30** (running total as of
today's inbox-triage run; `BACKLOG.md`'s entry itself still says 13,
that's just promotion lag — the real count is higher), **8 real
UptimeRobot outages with no matching Render alert at all**, roughly 2.5
alerts/day sustained over 9 days. One data point already on record: a
2026-09-05 crash (instance `8kp2j`) happened at 8:05 AM local, minutes
after a manual redeploy — the first time a crash has correlated with a
fresh deploy rather than steady-state traffic, worth keeping in mind while
reading the logs. Also worth knowing: Google Search Console flagged a
brand-new "Server error (5xx)" indexing-blocking reason for the first time
on 2026-09-06, timed close to one of the outages above — unconfirmed
(that dashboard is auth-walled from this Routine) but a plausible sign
this crash-loop is now costing real SEO, not just brief outages. See
item 5 below for the Search Console side of that.

---

## 2. Search Console → Validate Fix (reslug fix)

**Step 1 — Search Console → the `redtaperecordings.com` property → Indexing → Pages.**
Look for the indexing-issue rows tied to the reslug fix (old permalinks
that previously 200'd with the wrong canonical, now correctly 301
redirecting) — `BACKLOG.md`'s entry doesn't name the exact report row, so
scan for whichever row(s) show validation status **"Not Started."** As of
the 2026-09-06 dashboard email, Search Console reported "Some fixes failed
for Page indexing issues... Crawled - currently not indexed" — that's
expected, not a new problem; the entry itself already predicted "don't
expect it to clear 100%."
**Step 2** — click **Validate Fix** on the still-"Not Started" row(s). The
underlying code fix has been live since 2026-08-31 (confirmed: reslugged
pages serve real content) — this is purely telling Google to re-check.

---

## 3. Render → confirm two Archive fixes are deployed (WO-80, delete_meeting_pages_by_slug)

**Step 1 — Render dashboard → Archive service (`rtr-deeplink-archive`) → Events/deploy history.**
Both fixes are confirmed present on `main` (re-checked 2026-09-05):
`archive/main.py`'s `/api/health` using `LIMIT 1` instead of `SELECT
count(*)` (WO-80), and `archive/db/crud.py:9337`'s
`delete_meeting_pages_by_slug()` deleting `SocialPost`/`MeetingPageThumbnail`
rows before the page (PR #577). Every service has `autoDeploy: false`, so
merging never shipped this automatically. Look for: whether the most
recent deploy is from **on or after 2026-08-30 13:28 PDT**.
**Step 2** — if the last deploy predates that, click **Deploy latest
commit**. No code change needed — this is "ship what's already on `main`."
(No new alerts matching either gap have shown up since 2026-09-03, which
is consistent with — but not proof of — this already being deployed.)

---

## 4. Render → confirm CivicClerk worker fix is deployed (WO-88)

**Step 1 — Render dashboard → `rtr-transcription-worker` and `rtr-transcription-worker-2` → Events/deploy history.**
The fix (`app/platforms/civicclerk.py:77`'s `_reconstruct_cdn_stream_url()`)
is confirmed present on `main`, but the auto-transcription retry path
imports the platform module in-process rather than calling the deployed
resolver's API — so a worker on a stale build still hits the bug
regardless of the resolver's own deploy state. There's no
`/admin/schema-info`-style endpoint for either worker, so this has to be
read off Render directly. Look for: whether either worker has deployed
**on or after 2026-08-31 13:14 UTC** (transcription job 1308,
`kaysville-ut-2023-04-28-city-council-work-session`, failed 3/3 with the
exact pre-fix symptom at that timestamp).
**Step 2** — if either service's last deploy predates that, redeploy it.
No code change needed.

---

## 5. GitHub → dismiss secret-scanning false positive

**Step 1 — GitHub → `mroconnell/rtr-deeplink` → Security tab → Secret scanning alerts.**
Find the alert on `tests/fixtures/civicplus/durham_agendacenter_citycouncil.html:103`
(flagged as a "Google API Key"). It's Durham NC's own public
`GoogleMapsKey`, embedded in a real fixture of Durham's own public
CivicPlus page — not an RTR credential. The alert's own "Public leaks"
section already lists this same key in five unrelated public repos that
scraped the same page, confirming it was public before this repo's
fixture captured it.
**Step 2** — dismiss the alert with reason **"Used in tests."** Nothing to
rotate; this just clears "Action needed" from the Security tab. This
Routine has no GitHub write access for this action, and dismissing a
secret-scanning alert is a judgment call, so it's left for a human either
way.

---

## 6. Search Console → "Reasons preventing pages from being indexed" (lower priority)

**Step 1 — Search Console → Indexing → Pages → "Why pages aren't indexed."**
Of the four categories from the original 2026-08-23 alert, three are
resolved/explained; **"Not found (404)"** is the one still open. Look for:
whether any of the previously-flagged reslugged old permalinks now show up
under "Page with redirect" (301) instead of "Not found (404)," now that
the redirect fix has landed (2026-08-31).
**Step 2 — also check for a brand-new category**: "Server error (5xx)"
appeared for the first time on 2026-09-06 (both a page-level and a
sitemap-level report, 21:41–21:46 UTC) — this Routine can't see which URLs
or how many, since that view is auth-walled. If real, this is a plausible
first sign the SIGABRT crash-loop (item 1 above) is costing real
indexing, not just brief outages — worth noting the URL count/pattern if
you find it, so a later session can fold it into item 1's entry.
**Do not touch**: the other two original categories ("Alternate page with
proper canonical tag," "Duplicate, Google chose different canonical") are
the deliberate, documented consequence of this app's own canonicalization
choices (`state_page.html`/`meeting_list.html`) — not bugs.
