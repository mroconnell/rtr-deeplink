# Dashboard checklist — 2026-09-14

Auto-generated by the scheduled dashboard-checklist Routine. Source: `BACKLOG.md`'s
"Needs a human" section and the "Search Console, structured data & SEO plumbing"
subsection. Each item below is a quick external-dashboard look — log in, read a
number, done. Everything that needed real judgment, a destructive action, or
access this routine doesn't have (drip Mac, Render Shell, admin API token) was
left out on purpose — those stay in `BACKLOG.md` for a person or an interactive
session to work through directly.

2 items today.

---

## 1. Render bandwidth cap — size the overage and check for a new driver

**Source:** `BACKLOG.md`, "Needs a human" → "Production actions only Ryan
should take" → "Render account bandwidth hit its 25 GB/month Pro-plan cap".

Two real Render alerts (confirmed against Gmail 2026-09-13):
"Approaching Bandwidth Limit" (>70% of 25 GB) on **2026-09-09 12:09 UTC**
(**05:09 PDT**), day 9 of the billing cycle, then "Reached the Bandwidth
Limit" (100%) on **2026-09-12 12:09 UTC** (**05:09 PDT**), day 12. Per
Render's own alert text, everything past that point bills at $15/100GB until
the cycle resets. Nobody has sized the real dollar overage or found what's
driving it yet.

**Step 1 — Render → Billing.** Go to
`https://dashboard.render.com/w/tea-d21a0h24d50c739htil0/billing`. Find the
"Monthly Included Usage" section and read the Bandwidth row/category
breakdown (the same table used to size the August bandwidth incident).
- If the overage is small and clearly video/transcript proxying (the obvious
  guess) → note the $ amount, no further action needed until it recurs.
- If it's driven by something else (a different service, an unexpected
  spike) → that's new information worth a `BACKLOG.md` note, since both of
  the previously-known causes (double-proxying through the public internet,
  fixed 2026-08-30; a wrong stated 5 GB vs. real 25 GB limit) are already
  fixed — this alert is a new, unexplained driver, not a repeat.

**Step 2 — bonus, same login, 30 seconds.** This morning's inbox-triage run
(2026-09-14) found the Archive service's "HTTP health check failed" alert
reappeared once, at **2026-09-13 17:59 UTC (10:59 PDT)** — the first time in
two weeks since WO-80's August fix, and about 30 hours after the bandwidth
cap was hit. Not confirmed as related (Render's usage dashboard is the only
place that could confirm it — that's this exact page). While you're here:
check the Archive service's event/deploy log around that timestamp for
anything unusual (a restart, a throttling note). If you see nothing, that's
also useful — it rules the correlation out. No action needed either way
beyond noting what you see; flagged as "watch, don't promote yet" in
`CLAUDE_INBOX_TRIAGE.md`.

---

## 2. Search Console — "Missing field" flags on Videos/Events structured data

**Source:** `BACKLOG.md`, "Reliability, ops & cost" → "Search Console,
structured data & SEO plumbing" → "New 'Missing field' flags — Videos
`uploadDate`, Events `startDate`".

Background: a 2026-08-21 fix stopped the site from emitting an *invalid*
date into a page's structured data — but a page with no parseable date now
emits *no* `uploadDate`/`startDate` field at all instead, which is exactly
today's new GSC flag. That trade-off was made in code but never sized
against real production data.

**Step 1 — Search Console → Enhancements → Videos.** Open Search Console
for redtaperecordings.com, go to Enhancements in the left sidebar, open the
"Videos" report. Look for the "Missing field: uploadDate" issue row and
read the affected page count.
- A small/flat count → low priority, the trade-off is working as intended
  for a handful of pages.
- A large or growing count → worth a backfill or per-adapter date-capture
  fix; flag it back in `BACKLOG.md` with the number.

**Step 2 — Search Console → Enhancements → Events.** Same report list, open
"Events" instead of "Videos". Look for "Missing field: startDate" and read
its affected page count the same way.

**Step 3 — optional, more precise, needs the ingest token.** If you have
`ARCHIVE_INGEST_TOKEN` handy, `curl -H "Authorization: Bearer
$ARCHIVE_INGEST_TOKEN" "$ARCHIVE_BASE_URL/internal/date-format-audit"`
gives the exact null/unparseable-date page count directly from the database
(no GSC crawl-lag) — the endpoint already exists for exactly this
(`archive/main.py:959`). Not required; the GSC read above is enough for a
morning glance.

---

*Everything else scanned in `BACKLOG.md`'s "Needs a human" section and the
platform/jurisdiction-coverage area was either a judgment call with no
dashboard step (e.g. the tier-3 staleness cutoff, the Atlantic City
CITISTAT queue-or-not call, the PA PUC minting question), a destructive
production action (page deletes, `jurisdiction/override` writes, bulk
backfill script runs), or needed access this routine doesn't have (the
YouTube-drip Mac, a Render Shell, `ARCHIVE_INGEST_TOKEN`) — all of those
stay in `BACKLOG.md` for direct, in-person follow-up rather than a quick
click-through. None of the "Needs a human" entries showed evidence of
already being resolved.*
