# WO-360: West Virginia — fill the 103 placeholder municipal domains from the county sites, re-sweep the 18 corrected counties, and stand up the WV Board of Education

Read `preamble.md` (all standing rules), `full_wo320_323_passive_neither.md`, `full_wo337_338_addendum.md` first. Your WO number is WO-360. Branch `claude/wo360-west-virginia`.

## Why (Ryan + Breadth, 2026-09-13)
WO-337 found 54 WV towns sharing the `local.wv.gov` placeholder as their domain. Breadth then found that `https://www.wv.gov/local-government/<county>` links each county's real website: 22 of 54 county domains were wrong in our file and are now corrected in rtr-business (18 rows got the real domain with the old one moved to alternates and the reject reason cleared; 4 transcribed rows got it as an alternate). Each county site lists its municipalities with websites (e.g. `https://www.berkeleywv.org/341/Municipalities` → cityofmartinsburg.org, hedgesvillewv.us). 103 WV `us:place` rows have no real site: 57 carry `local.wv.gov`, 46 are blank.

## Part A — county-to-municipality walk (domain fill)
Walk the 55 WV county sites (domains as now in the research file), find each site's municipalities page (nav/hop scorer; one hop), extract municipality name → website links, match to WV `us:place` rows by name AND state (never by token; watch same-name towns across counties), fill `domain` where blank or `local.wv.gov` (old placeholder to alternate_domains per the never-blank rule), record the county page as evidence. Table: counties walked | municipality links found | matched to a row | domains filled | unmatched (named). Then run passive v2 (phases 1–3 + `verify_hub()` with the deeper walk) on every row whose domain you filled, and on the 18 corrected counties. Standing rules apply (empty listing → another hub; walk to 3 videos before an off-mission reject; >90 min queues; old meetings get pages). Hand-read gate; ingest tier 1; queue tier 3 after the owner pin; tier 2 → leads.

## Part B — the WV Board of Education
Breadth added research row `rtr:us:wv:west-virginia-board-of-education` (domain wvde.us) and one drip lead (August 2026 meeting, YouTube live). Its events archive `https://wvde.us/events-archive?f%5B0%5D=event_type%3A17` lists 9 more meetings (Nov 2025–Jul 2026) with YouTube live links. Do: (1) make sure the minted id resolves in the registry the way other `rtr:` mints do (follow `scripts/apply_pin_worklist.py`'s ok-mint path from WO-298 if the resolver needs a registry row; do not invent a new mechanism); (2) pins: `wvde.us` host → that id, and per-video `youtube:<id>` pins for the August meeting and the 9 archive meetings (read the archive page; never fetch YouTube); (3) append the 9 as drip leads (single_video, verified=false, source_wo=WO-360) via a file for Breadth; (4) record `https://www.wv.gov/agencies` as a state-agency directory source in the methods section (a lead for the eventual state-level tier, not worked here).

## Rules
Never fetch YouTube; never download media; research rows under §158 with `wo360_jc_applied_gov_ids.txt`; never commit in rtr-business (list files); WO-355/356/357/358 run concurrently — never touch rows that are not yours; wait on the lock; foreground runs, per-call timeout < 10 min, per-fetch 20 s / 3 MB; never idle on a monitor; never stop for scope; if budget runs out stop at a part boundary with exact counts.

## Report (Ryan's shape)
Purpose reminder; Part A tables (walk table; passive tier table for the filled rows and the 18 counties); Part B table (pins written, leads appended, mint state); video split; hand-read count + wrong; deploy line (pins → resolver deploy); rtr-business files; bold takeaway. BACKLOG_DONE entry, methods section as a separate file, five gates, PR, merge on green.
