# WO-363 — queue Excelsior MN and Brookline MA (WO-358 probe rejects reversed by Ryan)

Read scratchpad/briefs/preamble.md first (it is in the conductor's scratchpad, same directory as this file). Your worktree has no .env; the shared checkout's is at /Users/mroconnell/Documents/rtr-deeplink/.env — load it by explicit path if you need a token, and never print any value. Never fetch youtube.com. Never download a media file (ranged 64 KB GETs only). All runs in the foreground, per-call timeout under 10 min.

## Why
WO-358 ran the duration probe over 136 stale-label rows and dropped two meetings as "reject-dead" because the probe had no recipe for their media shape. Ryan's decision (via Breadth): "the duration probe should not remove a meeting if it would add to the archive's breadth; add those meetings back to the queue." Neither government has any Archive page, so both add breadth. Standing rule from now on: a probe failure is not a reject when the government has no transcript yet — queue it and let the transcriber decide.

## The two rows
1. Excelsior city, MN — gov_id us:place:2720078. CivicClerk page https://excelsiormn.portal.civicclerk.com/event/3241/media (Parks and Recreation Commission, 2026-08-11). Its video is a Cablecast show on the shared LMCC host: https://reflect-lmcc.cablecast.tv/CablecastPublicSite/show/57831?site=1. Sidecar row says: "no probe recipe for this media shape".
2. Brookline town, MA — gov_id us:cousub:2502109175. CivicClerk page https://brooklinema.portal.civicclerk.com/event/16635/media. Sidecar says its media is a Zoom Gov cloud recording share link (https://brooklinema.zoomgov.com/rec/share/...). The transcriber has no Zoom path as far as the conductor knows — verify that in app/platforms/media_probe.py and worker/main.py before deciding anything.

## Do this, in order
1. Read scripts/wo149_finish_tier3.py (top docstring, ~line 22) — it handled exactly this "CivicClerk wrapper page with no probe recipe" case before. Follow its pattern for what goes in the queue line: the CivicClerk page URL or the inner video URL, plus the second tab-separated column if the existing lines carry one. Look at scripts/tier3_auto_transcription_queue.txt lines that already point at reflect-lmcc.cablecast.tv (show 57519 is queued) and at civicclerk pages, and match the shape exactly.
2. Excelsior MN: fetch the CivicClerk page and the Cablecast show page (HTML only, no media). Confirm the show is that Parks and Recreation meeting and a real meeting recording (title, date, duration if the Cablecast API gives it — do not download). Append one queue line. Add the probe sidecar row with verdict "queued" and reason "probe had no recipe; queued per Ryan 2026-09-14 breadth rule" (match the existing column layout exactly; the file has 13 columns now — check the header and the last rows). Write the pins in app/utils/jurisdiction_data/tenant_overrides.csv: `excelsiormn.portal.civicclerk.com,,us:place:2720078,fallback,wo363,"Excelsior city, MN -- ..."` and a per-video pin `reflect-lmcc.cablecast.tv,show/57831,us:place:2720078,fallback,wo363,"..."`. reflect-lmcc.cablecast.tv serves several Lake Minnetonka cities, so check it is in MULTI_GOV_HOSTS (app/utils/jurisdiction*.py; grep MULTI_GOV_HOSTS) and add it if it is not. Copy the match-column convention from existing cablecast pins (grep 'cablecast.tv' tenant_overrides.csv).
3. Brookline MA: first walk the brooklinema CivicClerk listing with the shared verifier (from app.platforms.passive_verify import verify_hub; max_listings=15, max_videos=3) looking for a meeting whose video is NOT a Zoom link (CivicClerk sometimes has real MP4/Cablecast/YouTube media for other meetings). If one exists with a real video, queue that meeting instead (same steps as Excelsior; hand-read the title to be sure it is a governing-body meeting). If every video on the listing is Zoom, apply Ryan's rule literally: append the queue line for event 16635, sidecar row verdict "queued" with the reason naming the Zoom share link, and say plainly in the report that the transcriber will report a failure, not a transcript, unless a Zoom recording path is built. Either way pin brooklinema.portal.civicclerk.com to us:cousub:2502109175 (fallback, wo363). Note: a different Brookline (NH, us:cousub:3301108100) and Brookline MA's YouTube pin (us:cousub:5002509475 — that is Brookline VT, check) already exist; do not touch them, but if the YouTube pin's evidence says "Brookline town" with the wrong state, report it, do not fix.
4. Run the five CI gates locally (ruff check, ruff format --check, python -m pytest -q -x, alembic check in archive/ and app/, scripts/check_backlog_done_headings.py). Write a short BACKLOG_DONE.md entry ("WO-363 ...") recording Ryan's rule and the two rows. If BACKLOG.md's queue-related entries or CLAUDE_DASHBOARD_CHECKLIST mention the probe reject rule, update them; rerun scripts/build_backlog_toc.py after any BACKLOG.md edit.
5. Commit on branch claude/wo363-queue-excelsior-brookline, open the PR with gh pr create (body file named wo363_pr_body.md in your private scratch dir; re-read the body after create), then merge yourself on green with gh pr merge --squash --delete-branch. If the PR shows CONFLICTING, rebase on origin/main (main's version wins where main deleted/rewrote; union only your own added lines) and push --force-with-lease. Never commit anything in ~/Documents/rtr-business.

## Report (Ryan's shape)
One paragraph reminding what we were doing. A table of the two governments: page URL, video shape, what was queued (URL), pins written, sidecar verdict. Video split line: "captions available, page live now: N" / "video, no captions, queued: N". A bold one-line takeaway. Say whether a deploy is needed (tenant_overrides.csv and MULTI_GOV_HOSTS are resolver code = yes; queue files = no). List anything you left undone.
