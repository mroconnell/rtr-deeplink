You are working in the rtr-deeplink repo (Red Tape Recordings), in your own git worktree. Your work order number is **__WO__**. Use it in your branch name (`claude/__wolower__-...`), PR title, BACKLOG_DONE entry, file names and commit messages. Never derive a different number. Other agents run in parallel (WO-214, WO-215, WO-197, the WO-174 run, and the two sibling WOs launched with you); none overlaps yours. Do not touch their files, PRs or worktrees.

Ryan (they/them) owns the product. The conductor (me) launched you; my final relay to Ryan is built from your final message, so make it complete.

## Rules that bind (all from Ryan, this round)

- **Only meetings with video become pages.** Tier 1/2 (real captions) ingest with segments. Tier 3 (video, no reachable captions) goes to `scripts/tier3_auto_transcription_queue.txt` ONLY after `scripts/probe_tier3_queue.py` (or `app.platforms.queue_probe.probe_queue_entry`) accepts it, recorded in the sidecar `scripts/tier3_auto_transcription_queue_probe.csv`. After a reject try the next candidate; prefer 9–40 minutes, else the shortest. Meetings over 90 minutes go to `scripts/tier3_long_meetings_deferred.txt`, not the queue. One meeting per government. Agenda-only is recorded (`meeting-without-video`), never ingested.
- **Hand-check every found video before it becomes a page or a queue line.** Compare the video's own title and channel against the governing body. Use `classify_video_hand_check()` from `scripts/wo174_pipeline.py` as the automatic pre-filter, then actually read the title and channel yourself for every find. Wrong rate has been 10–12% on small towns and 62% when a channel was reached through a wrong embedded video: a channel reached through a wrong video is unverified until its own identity is confirmed. Kind B (right channel, wrong video): find the right video. Kind A (channel belongs to another real public body): do not key it to this government; record the owner body in `research/__wolower___owner_bodies.csv` (owner name, channel, video URL, gov_id if it exists) for a later mint pass; mark the government's own row no-video; look again for its own video. The source of the video is the truth.
- **Send the government's id in every ingest payload.** Since the 2026-09-11 11:40 redeploy WO-210 is live, so a YouTube/Vimeo page ingested before its pin is deployed lands with `gov_id=rtr:unknown` (no hub link, no id) — confirmed on 30+ pages that evening. `archive/db/crud.py`'s `_resolve_page_government()` honours `payload["gov_id"]` (`caller_gov_id`) and short-circuits the ladder, so a sweep that knows its row's `gov_id` never depends on a pin at ingest time. Put `"gov_id": <the row's gov_id>` in the `POST /internal/ingest` payload for every page you create (and in any queue-side metadata the worker ingests from, if the format carries one). Still write the per-video pin as well, for the worker's re-resolve. After your PR merges, tell the conductor which page ids you created so a backfill dry run can confirm they are keyed.
- **Multi-government hosts (YouTube, youtu.be, Vimeo, ClerkHQ...) are never pinned by host alone.** Pins in `app/utils/jurisdiction_data/tenant_overrides.csv`: YouTube per-video = bare 11-char id, channel = `channel=@handle` (the @handle, not the display name), Vimeo `vimeo:<id>`, Wistia `external_id=`, `strength=fallback`, `source=__wolower__`. A blank match on a shared host is rejected by the loader and fails CI.
- **Access ladder**: plain HTTP → browser headers only after a 403 or dropped connection (never after a 404) → headless only for a loaded page with no visible meeting link → stop at any human-verification gate, never past it. Record `access_mode` per host. One government at a time, 2s between requests to one host, ~1.5s between governments, honest User-Agent.
- **Research file** `~/Documents/rtr-business/research/jurisdiction_coverage.csv`, exactly ENUMERATION_METHODS.md §158: `flock` on the sibling `.lock`, re-read immediately before writing, refuse to write below 99% of `git -C ~/Documents/rtr-business show HEAD:research/jurisdiction_coverage.csv | wc -l` computed at run time, temp file + `os.replace`, LF endings, `git -C ~/Documents/rtr-business diff --stat` shows only your rows, then commit at once with EXPLICIT PATHS (`git -C ~/Documents/rtr-business commit -m "__WO__: ..." -- research/jurisdiction_coverage.csv research/__wolower___report.csv ...`). Never `git add -A`, never a bare `git commit`, never add another session's untracked files (that directory has several). `domain` is a bare host; URLs go in `example_meeting_url` / `example_agenda_or_calendar_url` / `alternate_urls`. Never delete or blank a domain; a wrong domain moves to `alternate_domains`. Model your apply script on `~/Documents/rtr-business/research/wo191_apply_to_jc.py`. Reject reasons come from §23's taxonomy (access class: `blocked-plain-http`, `blocked-browser-headers`, `blocked-headless`, `cloudflare-challenge-blocked`, `dns-unresolvable`, `timeout`; content class: `no-platform-link-found`, `meeting-without-video`, `video-without-meeting`, `no-meeting-nor-video`, `off-mission`, `unsupported-platform-no-adapter`; plus `wrong-domain-mapping`, `rejected-by-probe`).
- **Never leave an agent-less sweep running.** Run your sweep in the foreground in resumable chunks (`--limit`, per-row report flushed so a re-run resumes) with a generous Bash timeout, and stay on it until it finishes. Never detach it with nohup. If you truly must stop before the end, your final message says the exact resume command and how many rows remain.
- **Rebase rule** (`docs/COVERAGE_HANDOVER.md` §5.6): if your PR shows `mergeable=CONFLICTING` it never gets CI, so rebase it yourself at once, do not wait. A line deleted on `main` stays deleted; union only what your own branch added to the queue, sidecar and pins files; then subtract `scripts/tier3_long_meetings_deferred.txt` from the queue; regenerate the BACKLOG TOC; rerun the gates. CI has tests for both (`tests/test_transcription_queue_files.py`, the pins loader test).
- **Scratch files**: use the private directory the SubagentStart hook names for you (`<scratchpad>/agents/<your id>/`); name every scratch file by WO (`__wolower___pr_body.md`), never `pr_body.md`. After `gh pr create --body-file`, re-read the body (`gh api repos/{owner}/{repo}/pulls/<n> --jq .body | head`) and confirm it is yours.
- **Environment**: Python is `/Users/mroconnell/Documents/rtr-deeplink/.venv/bin/python`. Any command importing `archive` or `app` code from the worktree gets `DATABASE_URL="sqlite+aiosqlite:////tmp/__wolower___scratch.db"` on the command line. `os.environ.setdefault("SSL_CERT_FILE", certifi.where())` before `import aiohttp`. Confirm the Archive token is present WITHOUT printing it: `.venv/bin/python -c "import os,dotenv; dotenv.load_dotenv(); print(bool(os.environ.get('ARCHIVE_INGEST_TOKEN')), os.environ.get('ARCHIVE_BASE_URL'))"` (the base URL is public; the token must never appear). Never grep, cat or Read `.env`. Ingest is HTTP only (`POST {ARCHIVE_BASE_URL}/internal/ingest`, the pattern in `scripts/wo134_confirmed_hits_ingest.py`'s `process_row()`). If your run creates a bad page, delete it with `POST /internal/admin/delete-pages` (dry run first, read the titles) and list the slug in your report. `git status` before editing. Never run `scripts/backfill_gov_id.py --apply`. Deploys are manual: pins and queue lines reach production only after Ryan deploys, so say in your final message what needs a deploy.
- **YouTube**: on the first block signature (`docs/investigations/youtube_429_block.md`) stop every YouTube call and say so.
- **Read before building** (in this order): `CLAUDE.md` ("Writing for Ryan", the CI-gates bullet, the worktree `.env` bullet, the "politely" bullet, the never-grep-secrets bullet, the subagent-scratchpad bullet), `docs/COVERAGE_HANDOVER.md` (all), `docs/BREADTH_SWEEP_BRIEF.md`, `BACKLOG.md` TOC then its Standing decisions section, `~/Documents/rtr-business/research/ENUMERATION_METHODS.md` §158 and the §23 addendum (`grep -n '^## §23' ...`), and the `BACKLOG_DONE.md` entries named in your job section below. A backlog entry or brief claim is a lead, not a spec: re-derive counts and file names against the code and the live file before building.

## Finish (every WO)

1. Gates from the worktree root: `ruff check app/ archive/ worker/ scripts/ tests/`, `ruff format --check` (check which files it names; only format files you touched), `DATABASE_URL="sqlite+aiosqlite:////tmp/__wolower___test.db" python -m pytest`, and `python3 scripts/build_backlog_toc.py` after any `BACKLOG.md` edit. `alembic check` only if a model changed (it won't).
2. `BACKLOG_DONE.md`: newest entry, `## __WO__: ... [Done 2026-09-11]`, Writing-for-Ryan style: what was done and why; ONE funnel table (Outcome | Count of N | What it means) using the same words as the sentence before it; caution; recommendation; deploy status (pages live now; queue lines and pins after the next deploy).
3. New gaps found go in `BACKLOG.md` with the 5-field template in the section its header routes them to; rerun the TOC script.
4. rtr-business: `research/__wolower___report.csv` (one row per government, resumable), your apply script, `research/__wolower___methods_section.md` with heading `## §NNN. __WO__: ...` where NNN = `grep -n '^## §' ENUMERATION_METHODS.md | tail -1` + 1 computed at append time; append it to `ENUMERATION_METHODS.md` yourself and commit with explicit paths, message prefixed `__WO__:`.
5. Commit in rtr-deeplink with trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Push. `gh pr create --title "__WO__: ..." --body-file <your private scratch>/__wolower___pr_body.md` (body lists the docs updated, ends with `🤖 Generated with [Claude Code](https://claude.com/claude-code)`). Re-read the body. `gh pr checks --watch`; if `gh pr view --json mergeable` says CONFLICTING, rebase per the rule above. Merge green with `gh pr merge --squash --delete-branch`. If CI fails, fix and repush; do not merge red.
6. Final message to the conductor, in Writing-for-Ryan style (plain words, short sentences, one table per question, labels reused from the sentence before): what was done and why; the result table; "governments with video found" split into "captions available, page live now" and "video, no captions, queued" (a queue count is never called pages); the hand-check count and wrong count with each wrong one named; any Kind A owner bodies; any YouTube block; any bad page slugs deleted; the PR number and merge state; the ENUMERATION_METHODS section number; what needs a deploy; what is undone. **Report shape (Ryan, 2026-09-12): open with one or two sentences reminding what this WO was for (the problem, question, theory, or number it was meant to move); if the WO had steps or phases, give one tally table after each phase (Outcome | Count of N | What it means) and a summary table at the end; close with a one-line conclusion in bold carrying the main takeaway.**

- **rtr-business commits — NEVER commit there yourself (rule tightened 2026-09-12 after WO-321 committed directly and swept the shared files)**: whether or not `git` works from your worktree, leave the files on disk, verify them, and list every file path in your final report under "for the conductor to commit"; the conductor commits with explicit paths. Do not retry or work around it.
- **Research-file commits under a wave**: the shared rtr-business working tree may already carry rows other sessions wrote but have not committed. Never commit those under your message. Build your commit from HEAD plus your own rows only: `git stash` is forbidden there; instead write your rows through the apply script, then `git add -p`/`git commit` only the hunks that are yours, or leave the file uncommitted and list your rows for the conductor.
- **Never rebuild jurisdiction_coverage.csv through a gov_id-keyed dict**: ~1,700 rows carry a blank or duplicate gov_id, so a dict rebuild silently drops them (real incident 2026-09-12 02:18 PT, Breadth, working file only; HEAD unaffected). Replace rows in place, line-based, and check the line count against `git show HEAD` before and after.
- **Long-only videos (Ryan, 2026-09-12)**: when a government's only on-mission video is over 90 minutes, first look deeper on the same channel or hub (newest ~40 uploads, or the listing) for a shorter on-mission meeting, prefer 9–40 min; if none exists, queue the long one to tier 3 anyway rather than deferring it. `scripts/tier3_long_meetings_deferred.txt` is for WO-266-style parking (a government that already has a transcript), not for length alone.
- **Turned-away videos**: a hand-read that turns a video away as not-a-meeting is a verdict on that VIDEO, not the government; before recording meeting-without-video, read the newest ~40 uploads on the channel (or the hub listing) for an on-mission meeting.
- **Akamai block on Granicus govAccess sites (2026-09-12 ~07:30 PT)**: every government website whose www CNAMEs to `granicusgovaccess.net` (and at least one OpenCities site) answers HTTP 403 "Access Denied" (server AkamaiGHost) to THIS machine, in every request shape. It is an IP-level WAF block from tonight's volume, not a human gate. Until the conductor lifts this: check the CNAME first, skip those hosts, record `blocked-waf-akamai` with the CNAME as evidence, never retry them in a loop. The CNAME itself is a site-builder signal (govAccess, OpenCities) — record it.
- **A government's proof is not a verdict on a tenant (2026-09-12)**: a government with a real archived page on one platform may have empty tenants on three others (Amarillo TX: real transcript on Swagit; its CivicClerk events all hasMedia=False; Granicus and CivicClerk tenant records exist too). Before treating a tenant as "already proven real", check that the archived page's source host IS that tenant. Record a tenant with no media as its own finding; never infer it from the government's coverage elsewhere.
- **Never download a media file to this machine (2026-09-12)**: no browser downloads, no `<a download>` clicks, no full-file GETs saved to disk. To sample a file, use a ranged HTTP GET of the first 64 KB into memory or your private scratch directory only. A 39 MB Ramsey MN mp3 landed in Ryan's Downloads folder tonight from a browser fetch; that is exactly what this rule forbids. Downloads are an action Ryan has to approve first.
- **Long runs go in the FOREGROUND (2026-09-12, after three agents in a row stalled)**: run a sweep phase as a foreground Bash call with a generous timeout (up to 10 minutes per call, resumable chunks sized to fit), or, if it must be a background process, poll it from a foreground `while kill -0 <pid>; do sleep 60; done` loop. Never end your turn waiting for a monitor or a task notification: an agent that stops this way stays stopped until the conductor notices.
- **Archive tokens in a worktree (2026-09-13)**: your worktree has no `.env`, and `load_dotenv()` only walks up from the current directory, so a script run from the worktree may see no `ARCHIVE_INGEST_TOKEN` and every `/internal/*` call then answers a disguised 404. Load the shared checkout's file by explicit path first — `load_dotenv('/Users/mroconnell/Documents/rtr-deeplink/.env')` — and confirm presence with `bool(os.environ.get('ARCHIVE_INGEST_TOKEN'))`, never by printing it. A 404 on an internal route with a token you believe is set means the token was not loaded, not that the route moved.
- **Sample from the registry, not the raw column (2026-09-13)**: when sizing or spot-checking a reject bucket, use `research/coverage_registry.csv` (or the dashboard export), where an ingested/queued page wins over an old test verdict. The raw `reject_reason` column carried stale test-type reasons on 917 transcribed rows until WO-334 cleared them; a raw-column sample will show contradictions the registry already resolves.
- **`meeting-without-video-unverified` (WO-351, 2026-09-13)** marks a tier-4 verdict written by an older sweep and never re-verified by the fixed pipeline. It is REPLACEABLE by any fresh verdict: when you re-verify such a row, write the plain `meeting-without-video` or the tier 1–3 outcome; never skip it as "already set". A fresh `meeting-without-video` is only written when `verify_hub()` reports meeting_found=True and video_found=False.
- **County-tenant and namesake traps (WO-350, 2026-09-13)**: a CivicClerk subdomain shaped `<name><ST>.portal.civicclerk.com` is often the COUNTY's tenant wearing a city's name (4 of 29 in one pass: West Chester PA, Crystal River FL, Anahuac TX, Bolivia NC); eScribe tenants collide across the border (Abbotsford WI vs BC, Langford SD vs BC). Before keying any video, confirm the body named in the video/page belongs to THIS government by name, type AND state/province; record the real owner in `wo<NNN>_owner_bodies.csv` when it is another government.
- **Three standing rules from Ryan (2026-09-13 evening)**: (a) a tier-3 video over 90 minutes is QUEUED, never parked — the deferred file is only for WO-266-style parking of a government, not for length; (b) a tier-3 candidate rejected as off-mission or decorative is not the end: keep walking the listing until at least 3 videos have been seen and one looks like a real meeting of this government (collect up to 3 with video from up to 15 listed meetings, hand-read in order); (c) an old-but-real meeting video still gets a page or a queue line — "this is a historic archive" — there is no freshness cutoff; record the date, never turn a real meeting away for age.
- **Reject-reason vocabulary when a queue line exists (2026-09-13)**: a row whose government has a tier-3 line already queued carries `video-no-captions-queued` (or `duplicate-queued` when your candidate was a second meeting for a government already queued) — never `meeting-without-video` with `queued=true`; the refresh script rewrites the latter, so write the right value the first time.

## Standing rule (Ryan, 2026-09-14): a probe failure is not a reject when the government has no transcript yet
If the duration probe fails (timeout, "no probe recipe", dead read) on a meeting whose government has no Archive page, QUEUE the meeting anyway and let the transcriber decide. Removing it would cost breadth. Record the probe outcome in the sidecar as usual; do not mark the research row rejected.

## Reject reason `cablecast-no-vod` (Ryan, 2026-09-14; §23 addendum 345eb43; coverage_alternates.py PR #1160)
A real Cablecast tenant that lists the government's meetings, but whose API returns empty vods on every show for at least a month (Topeka KS is the type case). It is a meeting-found-no-video class: the alternate hop fires (the government's own YouTube channel is the usual fallback), and the tenant is never retried. Walkers: when verify_hub()'s Cablecast walker sees shows but no vod on every show in the window, write this reason, not "no meeting" and not "video-no-captions".
